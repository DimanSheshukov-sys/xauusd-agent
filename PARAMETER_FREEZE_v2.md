# PARAMETER FREEZE v2 — XAUUSD Elliott Trading Agent

**Статус: PRE-REGISTERED, действует с 2026-09-16.** Заменяет `PARAMETER_FREEZE_v1.md` в частях,
перечисленных ниже; всё остальное из v1 остаётся в силе без изменений.

Решения приняты автономно (пользователь делегировал полномочия: «сделай всё самостоятельно»).
Основание — `reports/EXPERIMENT_v2.md`, `reports/WALKFORWARD_v2.md`, `reports/EXPERIMENT_v3.md`.

> ⚠️ **Честная пометка о selection bias.** Правила v2 сформированы **по итогам** прогона на периоде
> 2022-01-24 → 2026-09-15. Поэтому все цифры кандидата v2 на этом периоде — **in-sample по выбору правил**
> и оптимистичны. Единственное валидное подтверждение — **forward OOS** (§4 ниже, требование §64).

---

## 1. Изменения относительно v1

| № | Что | v1 | **v2** | Основание |
|---|---|---|---|---|
| 1 | Production geometry | не выбрана (расхождение §47 vs §25/§82) | **`G_BASE47`: SL 2.5 ATR / TP 1.0R / max hold 96 баров** | §25/§82 (1 ATR / 1.5R) убыточна после издержек: Exp −0.082R, PF net 0.876, N=243. G_BASE47: Exp +0.156R, PF 1.373, p=0.043, 11/11 критериев §60 |
| 2 | Cost/R | не регламентирован | **новый gate `G14a`: round_trip_cost / R ≤ 0.05** | при 1 ATR cost/R = 0.07 → gate не пройден; при 2.5 ATR = 0.029 → пройден. Требует правки канона §10 |
| 3 | SHORT-ветки B05–B07 | активны (P1) | **DISABLED (P0)** | N=20, PF net 0.706, Exp −0.174R; B06 значимо отрицательна (p=0.021). Канонные §16 (PF 2.25, N=15) и §48 не воспроизведены |
| 4 | B03 W3 CLASS A (§13 high-conviction) | активна, приоритет 1 | **CONTEXT_ONLY** | CLASS A: PF net 0.947, Exp −0.030R, N=49 против канонных PF 2.217 |
| 5 | B04 Extended-W3 + RSI (§24) | P0 | **активная LONG-ветка, P1** | N=89, Exp +0.286R, PF 1.697, p=0.011 — сильнейшая изолированная ветка |
| 6 | Event filter (§43) | не реализован | **ON: FOMC decision + NFP, окно ±2h, блокирует только новые входы** | 93 события, 1.36% баров; абляция: Exp +0.2023R → **+0.2204R** |
| 7 | Wyckoff (§18/§51, gap D15) | не определён | **формализован, CONTEXT ONLY** (gate запрещён §19) | vol_ratio ≥ 1.30 на окне [P4−10 … P4+3] к базе 200 баров + stopping ≥ 0.50. Диагноз: True N=42 PF 1.594 / False N=99 PF 1.467 — слабый наклон, gate не оправдан |
| 8 | §26 SL-совместимость | `tight` (временное) | **`tight` — канонично** | трактовка `wide` даёт N=0 (неторгуема при 1 ATR), STRUCT-exit не наступает никогда |
| 9 | Набор торгуемых веток | B01–B07 | **LONG_ONLY = B01, B02, B04** (B03 — контекст) | SHORT отключён (п.3); WF-OOS LONG_ONLY Exp +0.205R, Net +25.0R, p=0.020 |
| 10 | Адаптивный отбор конфигурации | не применялся | **ЗАПРЕЩЁН** | WF с отбором внутри train: Exp выше (+0.316R), но Net R ниже (+18.0R против +25.0R) — повторяет вывод §48 «FROZEN > ADAPTIVE» |
| 11 | Данные | PAXG как source of record | **PAXG = directional/robustness стенд; для production-валидации нужен спот XAUUSD M15** | шум базиса PAXG−GC=F = 16.2 USD = **1.98 × ATR(M15)**; совпадение сигналов 2 из 9; при этом корреляция доходностей 0.9614 (M15) / 0.9288 (H1) |
| 12 | Swing k | 2.0 | **2.0 (без изменений)** | чувствительность: k=1.5 и k=3.0 дают тот же знак edge; k не участвует в отборе (защита от мета-оверфиттинга) |

## 2. Замороженная конфигурация v2 (полностью)

```text
DATA          : XAUUSD M15 (спот) — до его получения: PAXGUSDT M15 без выходных [PROXY]
SWING         : causal ZigZag, K = 2.0 × ATR(14, M15), min_bars отключён, ATR на баре-экстремуме
STRUCTURE     : IMPULSE_W5 (W1–W4 подтверждены) → forecast W5; ABC/Failed Fifth не торгуются
VALIDITY      : R1 W2 не ретрейсит 100% W1 • R2 W4 не заходит на территорию W1 • R3 W4 короче W3
                R4 «W3 не самая короткая» = PENDING (причинно непроверяема)
W3 CLASS      : A <1 | B 1–1.618 | C ≥1.618 (измеряется на confirm_idx пивота P4)
BRANCHES      : B01_LONG_CORE (Fib ON), B02_LONG_EMA200_FIBOFF, B04_LONG_EXTW3_RSI (RSI H1 > 50)
PRIORITY      : B04 → B01 → B02  (специфические раньше общих; на системный итог не влияет)
REGIME (G07)  : D1 close > D1 EMA200 (последний ЗАКРЫТЫЙ дневной бар) — REQUIRED для LONG
CONFIRM (G08) : B04 — RSI(14, H1) > 50; Fib ON для B01: 0.382 ≤ ret2 ≤ 0.786 и 0.236 ≤ ret4 ≤ 0.5
EVENT (G08a)  : вход запрещён в [FOMC/NFP − 2h … +2h]
TRIGGER (G10) : причинный Donchian(8) breakout на M15 (close за max/min предыдущих 8 баров)
INVALIDATION  : LONG — close < L4; SL-совместимость `tight` (SL ≥ L4), иначе WAIT_RISK
EXECUTION     : entry = open[i+1]; SL = 2.5 × ATR(14, M15); TP = 1.0R; max hold = 96 баров M15
OHLC RESOLVE  : TP и SL в одном баре → SL FIRST (§28)
COSTS         : 0.40 USD спред + 0.06 USD комиссия/сторону = 0.52 USD round-trip; gate G14a cost/R ≤ 0.05
SETUP AGE     : ≤ 200 баров M15, иначе S19 NO_TRADE
CONTEXT ONLY  : Wyckoff-флаг, W3 CLASS A, Schiff (не реализован), macro/DXY
```

## 3. Результаты кандидата v2 (RESEARCH SAMPLE — не доказательство)

| Метрика | Значение |
|---|---|
| N | 141 |
| Win Rate | 63.1% |
| PF net / gross | 1.573 / 1.712 |
| Expectancy net | **+0.2204R** |
| Net R | **+31.07R** |
| MaxDD | −9.93R (recovery 3.13) |
| t / p | 2.70 / **0.007** |
| Требуется N (95%) | 74 → **SAMPLE SUFFICIENT** |
| Издержки съели | 5.93R (19% от gross) |
| Период | 2022-02 → 2026-09 (PAXG PROXY) |

WF-OOS (отбор внутри train, 14 фолдов, сделки в 4): `LONG_ONLY|G_BASE47` статично — N=122, Exp +0.205R, Net +25.0R, p=0.020.

## 4. Forward OOS протокол (единственный путь к P3/P4)

```text
Старт           : первый бар после 2026-09-15 20:00 UTC
Данные          : спот XAUUSD M15 (при отсутствии — PAXG с пометкой PROXY, promotion невозможен)
Сигналы         : freeze в момент выдачи (S17 → S18, RULE 13), лог каждого решения по §72
Изменения правил: ЗАПРЕЩЕНЫ до завершения набора выборки (§64)
ACCEPT (P3)     : N ≥ 74 (для Exp ≈ +0.22R) AND Exp net > 0 AND p < 0.10
                  AND rolling ≥ 60% окон с Exp > 0 AND MaxDD ≤ 15R
NEEDS_MORE_DATA : 44 ≤ N < 74 при Exp > 0
REJECT          : Exp net ≤ 0 OR p ≥ 0.10 при N ≥ 74 OR rolling FAIL
Калибровка      : только после REJECT/ACCEPT, новой версией freeze (§63–65)
```

Ослабленный порог p < 0.10 для forward-выборки объявляется **заранее**, а не по факту.

## 5. Что осталось открытым

1. **Спот XAUUSD M15** — блокер P3 (прокси непригоден для structural-валидации: шум базиса ≈ 2 ATR).
2. CPI и прочие high-impact события не покрыты фильтром (нет исторического календаря без внешнего API).
3. Wave degree / вложенность волн (v1–v2: один уровень).
4. Альтернативные counts (§37) — политика не формализована.
5. Swaps/rollover в cost model.
6. Правки канона, которые нужны для непротиворечивости: §10 (добавить G14a cost/R), §25/§82 (геометрия),
   §26 (трактовка `tight`), §58 (статусы SHORT и W3 CLASS A), §60 (формула SAMPLE SUFFICIENT).

---

# ADDENDUM v2.1 — LIVE PAPER MODE и ОБУЧЕНИЕ (pre-registered ДО первого live-сигнала)

Дата: 2026-09-16 • Реализация: `web/paper.js` (браузер), `learn.py` (серверный отчёт), тесты `web/_papertest.js` (21/21 PASS).

## Правила live paper-трейдинга (заморожены)

| Параметр | Значение |
|---|---|
| Entry | open первого M15-бара ПОСЛЕ сигнального (§47); сигнал берётся только при decision = LONG и всех mandatory gates |
| SL / TP | геометрия G_BASE47: SL = 2.5 × ATR(14, M15), TP = 1.0R |
| Совпадение TP+SL в одном закрытом баре | **SL FIRST** (§28) |
| Structural invalidation | выход по close закрытого бара за уровнем (§11) |
| Max hold | 96 баров M15 → выход по close, reason TIME |
| Ручное закрытие | разрешено, reason MANUAL, **исключается из статистики правил** |
| Costs | 0.52 USD round-trip (0.40 спред + 2 × 0.06) |
| Проверка уровней | только по ЗАКРЫТЫМ барам; формирующийся бар даёт лишь live-стоп по текущей цене (защита между барами) |
| Журнал | append-only в localStorage браузера: закрытые сделки не изменяются (RULE 13); экспорт CSV кнопкой |
| Канонический журнал | серверный `data/forward/forward_signals.csv` остаётся источником истины для forward OOS |

## Классификация ошибок (§66) — эвристики заморожены

```text
WIN (net>0, MFE<1.5) • WIN-RUNNER-CANDIDATE (net>0, MFE≥1.5)
SL/SL_FIRST + MFE≥1.0  → EXECUTION ERROR: TP недосягаем / вход поздний
SL/SL_FIRST + MFE<0.2  → TRIGGER ERROR: вход против движения
SL/SL_FIRST иначе      → REGIME ERROR: шум против позиции
STRUCT → STRUCTURE ERROR • TIME → HOLD ERROR • MANUAL → вне статистики
```

## Калибровка (§63) и обучение (§65)

* tier по объёму выборки: <10 NO PARAMETER CHANGES • 10–29 SOFT RESEARCH • 30–49 NORMAL • 50+ FULL;
  любая калибровка — только после нового validation cycle;
* кандидаты порождаются при ≥3 ошибках одного класса (H-TP, H-TRIG, H-HOLD, H-VOL при ≥5, H-RUN при ≥3)
  и фиксируются как **pre-registered, НЕ применённые**;
* Final OOS для калибровки не используется (§64); отчёт — `reports/LEARNING_v1.md`;
* обучение в браузере показывает только диагностику: правила на странице не меняются никогда.

---

# ADDENDUM v2.2 — ФРАКТАЛЬНЫЕ СТЕПЕНИ ВОЛН (H4 / M15 / M5 / M1) — PRE-REGISTERED

Дата: 2026-09-16 • Статус: **анализ и отображение; торговые правила НЕ изменены** (канон §63–65: любая
торговая применение степеней требует отдельного walk-forward/OOS).

## Модель степеней

| Степень | Таймфрейм | Роль |
|---|---|---|
| D2 (primary) | H4 | основные волны импульса/коррекции |
| D1 | M15 | субволны текущей волны H4; execution-степень канона |
| D0 | M5 | суб-субволны текущей волны M15 |
| D-1 (micro) | M1 | микроволны текущей волны M5 |

## Правила разметки (заморожены)

1. Каждая степень размечается **независимо** причинным ZigZag с порогом K = 2.0 × ATR(14) **своего** ТФ
   (поэтому на мелких ТФ автоматически получаются мелкие субволны);
2. Вложенность выводится причинно по времени: текущая нога степени D(k) является отрезком, внутри
   которого живёт вся разметка степени D(k−1); цепочка читается как
   `H4:W3 ⊃ M15:W4 ⊃ M5:W2 ⊃ M1:W5`;
3. текущая нога степени = волна с номером `piv.length` (5 пивотов → W5 идёт сейчас; 3 пивота ABC → C);
4. согласованность (diagnostic): все 4 степени одного направления → «согласованы»;
   иначе «разнонаправлены». Это **контекст**, не гейт и не сигнал;
5. invalidation и trigger каждой степени — свои (по своему ZigZag/Donchian);
6. отображение: панель «Волны по таймфреймам» на LIVE + цепочка вложенности.

## Что запрещено

* использовать согласованность степеней как mandatory gate или trigger без нового pre-registration
  и walk-forward (§68–69);
* менять execution-степень канона (M15) и геометрию SL/TP;
* размечать степень по будущим барам (каждая степень использует только свои закрытые бары).

## Кандидат для будущей валидации (зафиксирован сейчас, не применяется)

* H-MTF-ALIGN: входить только при согласованности H4+M15+M5 по направлению → требует walk-forward 365/90
  на каждой степени и сравнения с текущим базовым правилом.

---

# ADDENDUM v2.3 — РИСК-И-UX ПАКЕТ (P0/P1 совета специалистов) — PRE-REGISTERED

Дата: 2026-09-16. Изменения НЕ касаются сигнальных правил (только издержки, аварийные лимиты,
честность статистики и UX), поэтому новой валидации сигнала не требуют.

1. **Динамические издержки:** спред берётся живьём из Binance `depth?limit=1` (PAXGUSDT, CORS `*`);
   при недоступности — консервативный fallback **0.60 USD**; комиссия 0.06/сторона.
   G14a (cost/R ≤ 0.05) и все R-метрики журнала считаются по живому спреду.
2. **Circuit breaker:** реализованный убыток дня (UTC) ≤ **−3R** → новые входы отключаются до конца дня,
   баннер на странице; журнал помечает состояние `circuit.open`.
3. **Always-valid p-value (mSPRT, τ=0.1R, σ=1.24R)** показывается в журнале и на LIVE:
   смотреть в журнал можно в любой момент без peeking-bias; классическое p остаётся для отчётов.
4. **Тик 25 c** (было 10 c) + полная пауза фетча/рендера в фоновой вкладке
   (`document.visibilityState`) + leader election между вкладками через BroadcastChannel:
   данные fetch'ит одна вкладка, остальные получают состояние сообщением.
5. **Sticky mini-bar** с решением, уровнем триггера, спредом и временем обновления.
6. **Интро для новичка** сверху главной + ссылка «как читать журнал».
